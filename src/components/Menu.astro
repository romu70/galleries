---
import Home from "../assets/Home.svg";
import GitHub from "../assets/GitHub.svg";
const base = import.meta.env.BASE_URL;
---

<div id="menu">
  <a href={base}>
    <Home/>
  </a>
  <a href={base + "/fonts"}>Fonts Gallery</a>
  <a href={base + "/streetart"}>Street Art Gallery</a>
  <a href={`${base}/about`}>About</a>
  <a href="https://github.com/romu70/galleries" target="blank">
    <GitHub/>
  </a>
</div>

<script>
  // let rssButton = document.querySelector("#rss-button");
  // rssButton?.addEventListener('click', onRssClick);

  // function onRssClick(this: HTMLElement, e: Event) {
  //   let url: string | undefined = "https://" + document.location.host + this.dataset.url;
  //   if(url) {
  //     navigator.clipboard.writeText(url);
  //     alert(`RSS link copied!\n\n${url}`);
  //     let element: HTMLElement | null = document?.activeElement as HTMLElement;
  //     element?.blur();
  //   }
  // }
</script>

<style>
  #menu {
    display: flex;
    flex-direction: row;
    align-items: flex-end;
    justify-content: end;
    gap: 0.5rem;
    visibility: collapse;
    width: 16rem;
  }

  #menu a {
    color:var(--main-color);
    text-align: center;
    text-decoration: none;
    font-size: 1.2rem;
    font-weight:200;
    padding: 1rem;
  }

  #menu a:hover,
  #menu a:focus {
    background-color: var(--main-color);
    color: var(--active-background-color);
  }

  #menu.expanded {
    margin-top: 0.5rem;
    visibility: visible;
    flex-direction: column;
    align-items:flex-start;
    background-color: var(--active-background-color);
  }

  svg {
    width: 1rem;
    height: 1rem;
  }

  @media (min-width: 768px) {
    #menu {
      display: flex;
      flex-direction: row;
      align-items: flex-end;
      align-content: end;
      width: 80%;
      visibility: visible;
      margin: auto;
    }
  }

  @media (min-width: 1024px) {
    #menu {
      width: 70%;
    }
  }

  @media (min-width: 1280px) {
    #menu {
      width: 60%;
    }
  }
</style>
